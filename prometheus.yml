@"
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "nlp_classifier"
    static_configs:
      - targets: ["app:8001"]
    metrics_path: /metrics
"@ | Set-Content -Path "docker\prometheus\prometheus.yml" -Encoding UTF8
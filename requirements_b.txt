# ── Phase B requirements ──────────────────────────────────────────────────────
# (includes all Phase A deps + pipeline extras)

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.1

# ── NLP preprocessing ─────────────────────────────────────────────────────────
# (unicodedata and html are stdlib — no extra install needed)

# ── Optional: Redis queue backend ─────────────────────────────────────────────
# redis[asyncio]==5.2.0

# ── ML frameworks — uncomment what your model needs ──────────────────────────
# torch==2.4.1
# transformers==4.45.2
# tokenizers==0.20.1
# onnxruntime==1.19.2
# scikit-learn==1.5.2
# numpy==1.26.4

# ── Testing ───────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
